# ---------- Auth (Recommended) ----------
# If both are set, the whole panel is protected by HTTP Basic Auth.
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=change-me

# ---------- Data backend ----------
# local: write ./data.json (ONLY for local dev; Vercel cannot persist it)
# webdav: read/write a single JSON file via WebDAV
# oss: read/write a single JSON object via Aliyun OSS
DATA_BACKEND=local

# local
DATA_PATH=./data.json

# webdav
WEBDAV_URL=
WEBDAV_USERNAME=
WEBDAV_PASSWORD=
WEBDAV_FILE_PATH=/deploy-manage/data.json

# aliyun oss
OSS_REGION=oss-cn-hangzhou
OSS_ENDPOINT=
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_BUCKET=
OSS_OBJECT_KEY=deploy-manage/data.json
